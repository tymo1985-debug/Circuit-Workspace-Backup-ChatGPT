/**
 * Circuit Workspace — shared/version.js
 * Единственное место, где хранится версия ОБОЛОЧКИ ХАБА (не версии модулей —
 * у Congress Project и Клиндария своя версия в их собственном коде).
 *
 * При каждом обновлении хаба меняйте только это значение — оно:
 *  1) автоматически отображается в шапке хаба (index.html читает его в рантайме,
 *     а не хранит номер версии как статический текст);
 *  2) автоматически меняет имя кэша service worker'а (CACHE_NAME), поэтому
 *     возвращающимся пользователям не покажется старая закэшированная версия —
 *     не нужно отдельно вручную поднимать "v1"/"v2"/"v3" в service-worker.js.
 *
 * `self` работает одинаково и в обычном <script> в браузере (self === window),
 * и при подключении через importScripts() в service worker'е — поэтому один
 * и тот же файл можно безопасно подключать в обоих местах.
 */
self.CW_VERSION = '0.8.2';

/**
 * Реестр модулей хаба.
 *
 * ЗАЧЕМ: раньше номера версий модулей были вписаны прямо в разметку плиток
 * index.html и молча расходились с реальностью (на плитке «Конгрессы» стояло
 * v4.17, тогда как модуль уже был v4.18). Теперь в разметке нет ни одного
 * номера версии — плитки читают версию отсюда в рантайме.
 *
 * ПРАВИЛО ВЫПУСКА: подняли версию внутри модуля — сразу поднимите её здесь.
 * Единственный источник версии внутри самих модулей остаётся прежним:
 *   congress-project → service-worker.js: APP_VERSION
 *   circuit-planner  → app.js: App.config.version
 *   pioneer-school   → js/app.js: APP_VERSION (и sw.js)
 */
// ВНИМАНИЕ: поле title здесь — только запасной вариант для отладки.
// Отображаемое название модуля берётся из локализации по ключу
// module.<id>.title (shared/i18n/common.js) и существует ровно там —
// в одном месте и для плитки хаба, и для шапки самого модуля.
self.CW_MODULES = {
  'congress-project': { title: 'Конгрессы',       version: '4.20.0' },
  'circuit-planner':  { title: 'Клиндарий',       version: '9.60.1' },
  'pioneer-school':   { title: 'Школа пионеров',  version: '1.6.0'  },
  'appointments':     { title: 'Назначения',      version: '5.1.0'  },
};
